// import { initScrollEffects } from './scrollEffects';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
)
// initScrollEffects();
